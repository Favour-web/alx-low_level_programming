#include <stdio.h>

/**
 * main - Prints the alphabetic
 *
 * Return: Always (Success)
 */
int main(void)
{
	int ch
	for (ch = 97; c <= 122; c++)
{
		putchar(ch);
}
	putchar(10); /* this is an ascii code for new line*/
	
	return (0);
}
