#include <stdio.h>

/**
 * main - Prints the alphabetic
 *
 * Return: Always (Success)
 */
int main(void)
{
	int ch
	for (ch = 97; c <= 122; c++)
{
		putchar(ch);
}
putchar(10);
	return (0);
}
